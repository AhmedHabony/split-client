import React, {Component} from 'react';

class CreateNoteSave extends Component {
    render() {
        return (
            <div className={'CreateNoteSave'}>

                <button
                    className={'CreateNoteSave__saveBtn'}
                > Save Note
                </button>

            </div>
        );
    }
}

export default CreateNoteSave;